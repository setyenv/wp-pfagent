{
  "schemaVersion": 1,
  "tools": [
    {
      "name": "list_files",
      "description": "List files in the virtual workspace. Pass `prefix` to scope (e.g. '/workflows/' for real workflows, '/templates/' for read-only templates, '/lib/' for reference docs). With no prefix, returns the top-level directory listing. Call it once at the start of workflow work to see what exists, then reach for search_files to find what you need inside those files.",
      "capabilityKeys": [
        "agent.vfs.list"
      ],
      "permission": "viewWorkflows",
      "sideEffect": false,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "list_files"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "prefix": {
            "type": "string",
            "description": "Optional path prefix. Defaults to '/'."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "search_files",
      "description": "Search the virtual workspace for a literal string and get back the matching lines, instead of reading whole files. Use it FIRST whenever you need to find something in the reference libraries: the node whose name you half remember, which entity exposes a trigger, how an existing workflow wires a step, where a variable is used. `pattern` is matched case-insensitively anywhere in a line; `prefix` scopes the search ('/lib/', '/workflows/', '/templates/'); `max_results` caps the hits (default 40). Each hit is { path, line, text }, so you can go straight to read_file on the one file that matters — or skip reading altogether when the line already told you what you needed.",
      "capabilityKeys": [
        "agent.vfs.read"
      ],
      "permission": "viewWorkflows",
      "sideEffect": false,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "search_files"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "pattern": {
            "type": "string",
            "minLength": 1,
            "description": "Literal text to look for, matched case-insensitively (not a regular expression)."
          },
          "prefix": {
            "type": "string",
            "description": "Optional path prefix to scope the search (e.g. '/lib/', '/workflows/'). Defaults to '/'."
          },
          "max_results": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "description": "Maximum number of matching lines to return. Defaults to 40."
          }
        },
        "required": [
          "pattern"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "read_file",
      "description": "Read a file's current contents. Common targets: '/lib/nodes.d.ts' (the verb library), '/lib/manage.d.ts' (the pfmanagement model surface — entities, events, actions, plus per-entity virtual CRUD), '/lib/variables.d.ts' (the variables declared in a specific workflow — pass workflow_id), '/workflows/<id>__<slug>.pfflow' (an existing workflow's source), '/templates/<slug>.pfflow' (a built-in template's source). The two libraries are generated and BIG (hundreds of kilobytes): read their first ~120 lines for the authoring model, use search_files to locate the exact declaration you need, and come back with from_line/max_lines to read just that region. A whole-file read of a library is almost never the right call; workflow and template sources are small and fine to read whole. Returns { source, draft, liveSource?, lastError?, workflowId?, window? }; `draft: true` means the user has unsaved changes in this file.",
      "capabilityKeys": [
        "agent.vfs.read"
      ],
      "permission": "viewWorkflows",
      "sideEffect": false,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "read_file"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Absolute path inside the virtual workspace (starts with /)."
          },
          "workflow_id": {
            "type": "string",
            "minLength": 1,
            "description": "Required when reading /lib/variables.d.ts (selects which workflow's variables to surface). Ignored for other paths. This is the workflow id exactly as returned by write_file / read_file / list_files (a uuid such as \"f281136a-5387-4a25-baad-3d2db3b2357e\") — never a number you made up."
          },
          "from_line": {
            "type": "integer",
            "minimum": 1,
            "description": "Optional. First line to return (1-based). Use it with max_lines to read a large generated file in windows instead of whole."
          },
          "max_lines": {
            "type": "integer",
            "minimum": 1,
            "description": "Optional. How many lines to return from from_line. The response then carries a `window` with fromLine, toLine, totalLines and hasMore."
          }
        },
        "required": [
          "path"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "write_file",
      "description": "Replace a workflow file with new contents. Compiles immediately; on compile error returns a { line, column, reason, hint } error and leaves the previous draft untouched, so you can fix-forward by calling write_file again. ON SUCCESS the customer's preview pane updates to show the new graph (cards + wires) — the underlying workflow in the library is NOT persisted yet (the customer commits by clicking Apply in the UI). For a brand-new workflow use the path /workflows/new__<slug>.pfflow. Do not write to /lib/* or /templates/* (read-only).",
      "capabilityKeys": [
        "agent.vfs.write"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "write_file"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1
          },
          "contents": {
            "type": "string",
            "description": "The entire new file contents in the workflow source language."
          }
        },
        "required": [
          "path",
          "contents"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "edit_file",
      "description": "Surgical string-replace edit in a workflow file. Provide an `oldStr` that occurs exactly once in the current file, and the `newStr` that replaces it. Use this when you want to change a small region without rewriting the whole file — much cheaper than write_file. Compiles after the substitution; on compile error returns a typed error and you can fix-forward. Like write_file, this updates a draft + preview; it does NOT persist to the customer's library.",
      "capabilityKeys": [
        "agent.vfs.edit"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "edit_file"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1
          },
          "oldStr": {
            "type": "string",
            "minLength": 1,
            "description": "Exact snippet to replace (must occur exactly once in the file)."
          },
          "newStr": {
            "type": "string",
            "description": "Replacement snippet."
          }
        },
        "required": [
          "path",
          "oldStr",
          "newStr"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "move_file",
      "description": "Rename a workflow file. Side effect: the workflow's name in the customer's library changes immediately and requires user confirmation. Only valid for /workflows/<id>__<slug>.pfflow paths; the id portion does not change, only the slug.",
      "capabilityKeys": [
        "agent.vfs.move"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "move_file"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "from": {
            "type": "string",
            "minLength": 1
          },
          "to": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "from",
          "to"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "delete_file",
      "description": "Delete a workflow. Side effect: the workflow is removed from the customer's library permanently — requires user confirmation. Only valid for /workflows/<id>__<slug>.pfflow paths. Templates and the library are not deletable.",
      "capabilityKeys": [
        "agent.vfs.delete"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "delete_file"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "path"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "pfm_get_contract",
      "description": "Bootstrap call to the wp-pfmanagement agent-ready service. Returns the type catalog (field types + accepted config keys), the EQL operator vocabulary, and the supported resource kinds (entity, record, action, application, module, group, role, page, business_rule, row_visibility, dialog, rest_message, scripted_route, oauth_connection) — wrapped as { content, contextForYou }. Read contextForYou for behavioural rules. DO NOT call again if you already have it in this conversation; it is large and stable.",
      "capabilityKeys": [
        "management.agent_contract"
      ],
      "permission": "viewWorkflows",
      "sideEffect": false,
      "phpService": {
        "filter": "projectflash_management_agent_api",
        "method": "agent_contract"
      },
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "pfm_list",
      "description": "List resources of one kind from wp-pfmanagement as full round-trippable payloads. `kind` must be one of: entity, record, action, application, module, group, role, page, business_rule. `filters` is optional and kind-specific (e.g. record uses { entity, query, limit }; business_rule accepts an entity_slug filter). Outer envelope is { content, contextForYou } and each item carries its own inner envelope. UI Policies are admin-only and not listable through this tool.",
      "capabilityKeys": [
        "management.agent_list"
      ],
      "permission": "viewWorkflows",
      "sideEffect": false,
      "phpService": {
        "filter": "projectflash_management_agent_api",
        "method": "agent_list",
        "argMapping": [
          "kind",
          "filters"
        ]
      },
      "parameters": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "entity",
              "record",
              "action",
              "application",
              "module",
              "group",
              "role",
              "page",
              "business_rule",
              "row_visibility",
              "dialog",
              "rest_message",
              "scripted_route",
              "oauth_connection"
            ]
          },
          "filters": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "required": [
          "kind"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "pfm_get",
      "description": "Load one wp-pfmanagement resource as a full round-trippable payload. `kind` selects the resource type; `ref` is the slug (for entity/action/application/module/group/role), composite \"entity:sys_id\" (for record), the page slug (for page), or the id (for business_rule — also accepts \"id:<id>\"). UI Policies are admin-only and not gettable through this tool.",
      "capabilityKeys": [
        "management.agent_get"
      ],
      "permission": "viewWorkflows",
      "sideEffect": false,
      "phpService": {
        "filter": "projectflash_management_agent_api",
        "method": "agent_get",
        "argMapping": [
          "kind",
          "ref"
        ]
      },
      "parameters": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "entity",
              "record",
              "action",
              "application",
              "module",
              "group",
              "role",
              "page",
              "business_rule",
              "row_visibility",
              "dialog",
              "rest_message",
              "scripted_route",
              "oauth_connection"
            ]
          },
          "ref": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "kind",
          "ref"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "pfm_apply",
      "description": "Persist one wp-pfmanagement resource. `kind` selects the resource type; `payload` is the full shape returned by pfm_get / pfm_list for that kind (round-trippable). Create vs. update is decided by the presence of slug/id in the payload. SIDE EFFECT — requires user confirmation. Construction only; firing actions / running workflows is a separate surface. Business Rules are LIMITED: you may create, update (including activate/deactivate via is_active), list, and get them — but DELETE is refused; to stop a rule, send is_active=false. When you CREATE a business_rule, the platform auto-creates a paired draft workflow and returns its workflow_id in the response; tell the operator they can ask you to edit it (it lives at /workflows/<workflow_id>__<slug>.pfflow). UI Policies are NOT exposed through this tool — they are admin-only.",
      "capabilityKeys": [
        "management.agent_apply"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_management_agent_api",
        "method": "agent_apply",
        "argMapping": [
          "kind",
          "payload",
          "options"
        ]
      },
      "parameters": {
        "type": "object",
        "description": "Canonical shape: { kind: '<entity|record|action|application|module|group|role|page|business_rule|row_visibility|dialog|rest_message|scripted_route|oauth_connection>', payload: { <kind>: {...} } }. The bridge also accepts flat shapes (top-level kind-named keys without an explicit `payload` wrapper) and auto-infers kind from a unique top-level resource key — useful when the LLM forgets the wrapper. For any kind you have not built before, call pfm_get_contract / pfm_list first: the returned contextForYou documents that kind's exact payload shape.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "entity",
              "record",
              "action",
              "application",
              "module",
              "group",
              "role",
              "page",
              "business_rule",
              "row_visibility",
              "dialog",
              "rest_message",
              "scripted_route",
              "oauth_connection"
            ]
          },
          "payload": {
            "type": "object",
            "additionalProperties": true
          },
          "entity": {
            "type": "object",
            "additionalProperties": true
          },
          "record": {
            "type": "object",
            "additionalProperties": true
          },
          "action": {
            "type": "object",
            "additionalProperties": true
          },
          "application": {
            "type": "object",
            "additionalProperties": true
          },
          "module": {
            "type": "object",
            "additionalProperties": true
          },
          "group": {
            "type": "object",
            "additionalProperties": true
          },
          "role": {
            "type": "object",
            "additionalProperties": true
          },
          "page": {
            "type": "object",
            "additionalProperties": true
          },
          "business_rule": {
            "type": "object",
            "additionalProperties": true
          },
          "row_visibility": {
            "type": "object",
            "additionalProperties": true
          },
          "values": {
            "type": "object",
            "additionalProperties": true
          },
          "sys_id": {
            "type": "string"
          },
          "numberSequence": {
            "type": "object",
            "additionalProperties": true
          },
          "formLayout": {
            "type": "object",
            "additionalProperties": true
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "options": {
            "type": "object",
            "description": "Optional apply options. Set options.force=true to consent to an unsafe reconcile plan (e.g. adding fields to an entity that already has records) AFTER reviewing the plan returned inside the pfm_reconcile_unsafe error. Never use force to drop or retype columns unless the user explicitly asked for that.",
            "properties": {
              "force": {
                "type": "boolean"
              }
            },
            "additionalProperties": false
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "pfm_delete",
      "description": "Delete ONE wp-pfmanagement resource by reference. `kind` selects the resource type; `ref` identifies the single target: the slug (entity/application/role/group/event), \"<application_slug>.<module_slug>\" (module), \"<entity_slug>.<action_slug>\" (action), \"<entity_slug>:<sys_id_hex>\" (record), or the id (row_visibility rule). SIDE EFFECT — requires user confirmation. Gated exactly like pfm_apply: you may only delete what your role may delete, and every deletion is audited. ONE call deletes ONE explicit target — there is no bulk/mass delete. Business Rules are NOT deletable here (delete is refused; to stop a rule send pfm_apply with is_active=false, which keeps the definition). Pages and UI Policies are not deletable through the agent. Deleting an entity also drops its records and form layout; a record delete clears inbound relations pointing at it.",
      "capabilityKeys": [
        "management.agent_delete"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_management_agent_api",
        "method": "agent_delete",
        "argMapping": [
          "kind",
          "ref",
          "options"
        ]
      },
      "parameters": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "entity",
              "record",
              "action",
              "application",
              "module",
              "group",
              "role",
              "event",
              "business_rule",
              "row_visibility",
              "dialog",
              "rest_message",
              "scripted_route",
              "oauth_connection"
            ]
          },
          "ref": {
            "type": "string",
            "minLength": 1,
            "description": "The single target: slug (entity/application/role/group/event), \"<application_slug>.<module_slug>\" (module), \"<entity_slug>.<action_slug>\" (action), or \"<entity_slug>:<sys_id_hex>\" (record)."
          },
          "options": {
            "type": "object",
            "description": "Optional delete options. options.force=true confirms an unsafe entity delete (e.g. one that still has records) after reviewing the returned plan.",
            "properties": {
              "force": {
                "type": "boolean"
              }
            },
            "additionalProperties": false
          }
        },
        "required": [
          "kind",
          "ref"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "create_variable",
      "description": "Declare a workflow variable so you can read or write it from the .pfflow source. Variables are the LAST RESORT — before reaching for one, exhaust every other source: a field already on the trigger event (`event.<field>`), a field on a record you Get from the data model, the output pin of an upstream node, a value the customer's request already contains. Only when none of these can produce the value (e.g. a fixed template string, a threshold the customer wants you to use, an EQL filter literal, a hardcoded email) do you declare a variable. Pass `name` in any human form — \"Estado buscado\", \"estadoBuscado\", \"estado_buscado\" all normalise to the same slug. Pick `type` from the enriched type system: string/text, number, integer, boolean, date, datetime, url, email, phone, currency, user, group, media, post, secret, file. Optional `value` becomes the default. ON SUCCESS the response includes `identifier` (e.g. `Estado_buscado`) and the exact getter/setter you must type in the .pfflow (`Estado_buscado$Variable$Get` / `...$Set`). The new variable lands in /lib/variables.d.ts automatically; call read_file('/lib/variables.d.ts', workflow_id) once after creating to refresh your view, then wire it.",
      "capabilityKeys": [
        "agent.vfs.write"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "create_variable"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "workflow_id": {
            "type": "string",
            "minLength": 1,
            "description": "Id of the workflow this variable belongs to, exactly as returned by write_file / read_file / list_files (a uuid such as \"f281136a-5387-4a25-baad-3d2db3b2357e\") — never a number you made up."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable variable name (any form: spaces, camelCase, snake_case). Normalised to a stable slug on storage."
          },
          "type": {
            "type": "string",
            "enum": [
              "string",
              "text",
              "number",
              "integer",
              "boolean",
              "date",
              "datetime",
              "url",
              "email",
              "phone",
              "currency",
              "user",
              "group",
              "media",
              "post",
              "secret",
              "file"
            ],
            "description": "Enriched type. Must match the Pin type wherever the variable will be wired."
          },
          "value": {
            "description": "Optional default value (string/number/boolean). Becomes the variable's `defaultValue` so the workflow has a sensible value on first read."
          }
        },
        "required": [
          "workflow_id",
          "name",
          "type"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "activate_workflow",
      "description": "Activate a workflow that is currently in draft so it starts firing on its trigger. Use this once the workflow is fully wired (you have already called write_file and compile succeeded) and the customer has confirmed they want it live. Side effect: future trigger events will execute the workflow — requires user confirmation. Pass the same path you used for write_file (e.g. /workflows/new__<slug>.pfflow or /workflows/<id>__<slug>.pfflow).",
      "capabilityKeys": [
        "agent.vfs.write"
      ],
      "permission": "manageWorkflows",
      "sideEffect": true,
      "phpService": {
        "filter": "projectflash_agent_vfs_bridge",
        "method": "activate_workflow"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Path of the workflow to activate (the one you wrote)."
          }
        },
        "required": [
          "path"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/vfs-and-bridges-end-to-end.spec.cjs"]
    },
    {
      "name": "wp_post_list",
      "description": "List/search WordPress posts, pages or any custom post type. Filter by type, status, author, search text, date order. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_post_list"
      ],
      "permission": "wpRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_post_list"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "post_type": {
            "type": "string",
            "description": "Post type slug: 'post' (default), 'page', or a custom type."
          },
          "status": {
            "type": "string",
            "description": "Status filter: 'any' (default), 'publish', 'draft', 'pending', 'private', or comma-separated."
          },
          "author": {
            "type": "integer",
            "description": "Filter by author user id."
          },
          "search": {
            "type": "string",
            "description": "Free-text search."
          },
          "per_page": {
            "type": "integer",
            "description": "Items per page (1-100, default 20)."
          },
          "page": {
            "type": "integer",
            "description": "Page number (default 1)."
          },
          "orderby": {
            "type": "string",
            "description": "One of date|title|modified|ID|menu_order."
          },
          "order": {
            "type": "string",
            "description": "ASC or DESC (default DESC)."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_post_get",
      "description": "Read one post/page/CPT by id (or slug + post_type): fields, terms and public meta. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_post_get"
      ],
      "permission": "wpRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_post_get"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Post id."
          },
          "slug": {
            "type": "string",
            "description": "Post slug (needs post_type)."
          },
          "post_type": {
            "type": "string",
            "description": "Post type when resolving by slug (default 'post')."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_post_create",
      "description": "Create a post/page/CPT. Defaults to draft. Content is sanitised. Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_post_create"
      ],
      "permission": "wpWriteContent",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_post_create"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "post_type": {
            "type": "string",
            "description": "Post type (default 'post')."
          },
          "title": {
            "type": "string",
            "description": "Post title."
          },
          "content": {
            "type": "string",
            "description": "Post body (HTML allowed, sanitised)."
          },
          "status": {
            "type": "string",
            "description": "draft (default)|publish|pending|private."
          },
          "excerpt": {
            "type": "string",
            "description": "Optional excerpt."
          },
          "author": {
            "type": "integer",
            "description": "Optional author user id."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_post_update",
      "description": "Update an existing post/page/CPT (title, content, excerpt, status). Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_post_update"
      ],
      "permission": "wpWriteContent",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_post_update"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Post id."
          },
          "slug": {
            "type": "string",
            "description": "Post slug (needs post_type)."
          },
          "post_type": {
            "type": "string",
            "description": "Post type when resolving by slug."
          },
          "title": {
            "type": "string",
            "description": "New title."
          },
          "content": {
            "type": "string",
            "description": "New body (sanitised)."
          },
          "excerpt": {
            "type": "string",
            "description": "New excerpt."
          },
          "status": {
            "type": "string",
            "description": "draft|publish|pending|private."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_post_trash",
      "description": "Move a post/page/CPT to the trash (not permanent deletion). Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_post_trash"
      ],
      "permission": "wpWriteContent",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_post_trash"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Post id."
          },
          "slug": {
            "type": "string",
            "description": "Post slug (needs post_type)."
          },
          "post_type": {
            "type": "string",
            "description": "Post type when resolving by slug."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_taxonomy_list",
      "description": "With no argument, list public taxonomies. With 'taxonomy', list that taxonomy's terms. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_taxonomy_list"
      ],
      "permission": "wpRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_taxonomy_list"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "taxonomy": {
            "type": "string",
            "description": "Taxonomy slug to list terms of (e.g. 'category','post_tag')."
          },
          "per_page": {
            "type": "integer",
            "description": "Max terms (1-500, default 100)."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_term_create",
      "description": "Create a taxonomy term (category, tag or custom). Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_term_create"
      ],
      "permission": "wpWriteContent",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_term_create"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "taxonomy": {
            "type": "string",
            "description": "Taxonomy slug."
          },
          "name": {
            "type": "string",
            "description": "Term name."
          },
          "description": {
            "type": "string",
            "description": "Optional description."
          },
          "parent": {
            "type": "integer",
            "description": "Optional parent term id."
          }
        },
        "required": [
          "taxonomy",
          "name"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_term_assign",
      "description": "Assign terms to a post. Terms may be ids or names/slugs. Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_term_assign"
      ],
      "permission": "wpWriteContent",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_term_assign"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "post_id": {
            "type": "integer",
            "description": "Target post id."
          },
          "taxonomy": {
            "type": "string",
            "description": "Taxonomy slug."
          },
          "terms": {
            "type": "array",
            "description": "Term ids or names/slugs.",
            "items": {
              "type": [
                "integer",
                "string"
              ]
            }
          },
          "append": {
            "type": "boolean",
            "description": "Append instead of replace (default false)."
          }
        },
        "required": [
          "post_id",
          "taxonomy",
          "terms"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_media_list",
      "description": "List the media library (filter by search text or mime type). Read-only.",
      "capabilityKeys": [
        "wpcore.wp_media_list"
      ],
      "permission": "wpMedia",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_media_list"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "search": {
            "type": "string",
            "description": "Free-text search."
          },
          "mime": {
            "type": "string",
            "description": "Mime filter, e.g. 'image' or 'image/png'."
          },
          "per_page": {
            "type": "integer",
            "description": "Items per page (1-100, default 20)."
          },
          "page": {
            "type": "integer",
            "description": "Page number."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_media_get",
      "description": "Read one media attachment's metadata (url, alt, dimensions). Read-only.",
      "capabilityKeys": [
        "wpcore.wp_media_get"
      ],
      "permission": "wpMedia",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_media_get"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Attachment id."
          }
        },
        "required": [
          "id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_user_list",
      "description": "List users (filter by role or search). Never returns passwords. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_user_list"
      ],
      "permission": "wpUsersRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_user_list"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "role": {
            "type": "string",
            "description": "Role slug filter."
          },
          "search": {
            "type": "string",
            "description": "Free-text search."
          },
          "per_page": {
            "type": "integer",
            "description": "Items per page (1-100, default 20)."
          },
          "page": {
            "type": "integer",
            "description": "Page number."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_user_get",
      "description": "Read one user's profile by id or login. Never returns the password. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_user_get"
      ],
      "permission": "wpUsersRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_user_get"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "User id."
          },
          "login": {
            "type": "string",
            "description": "User login."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_user_create",
      "description": "Create a user (username, email, role). Password is auto-generated and never disclosed. Role grants are guarded against privilege escalation. Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_user_create"
      ],
      "permission": "wpUsersWrite",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_user_create"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "username": {
            "type": "string",
            "description": "Login."
          },
          "email": {
            "type": "string",
            "description": "Email address."
          },
          "role": {
            "type": "string",
            "description": "Role slug (default site default)."
          },
          "display_name": {
            "type": "string",
            "description": "Display name."
          },
          "first_name": {
            "type": "string",
            "description": "First name."
          },
          "last_name": {
            "type": "string",
            "description": "Last name."
          }
        },
        "required": [
          "username",
          "email"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_user_update",
      "description": "Update a user's profile or role (role changes need promote_users and are escalation-guarded). Passwords are not changeable here. Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_user_update"
      ],
      "permission": "wpUsersWrite",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_user_update"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "User id."
          },
          "email": {
            "type": "string",
            "description": "New email."
          },
          "role": {
            "type": "string",
            "description": "New role slug."
          },
          "display_name": {
            "type": "string",
            "description": "Display name."
          },
          "first_name": {
            "type": "string",
            "description": "First name."
          },
          "last_name": {
            "type": "string",
            "description": "Last name."
          }
        },
        "required": [
          "id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_comment_list",
      "description": "List comments (filter by post, status, search). Read-only.",
      "capabilityKeys": [
        "wpcore.wp_comment_list"
      ],
      "permission": "wpComments",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_comment_list"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "post_id": {
            "type": "integer",
            "description": "Filter by post id."
          },
          "status": {
            "type": "string",
            "description": "all|approve|hold|spam|trash."
          },
          "search": {
            "type": "string",
            "description": "Free-text search."
          },
          "per_page": {
            "type": "integer",
            "description": "Items per page (1-100, default 20)."
          },
          "page": {
            "type": "integer",
            "description": "Page number."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_comment_moderate",
      "description": "Moderate a comment: approve, hold, spam, trash, or reply. Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_comment_moderate"
      ],
      "permission": "wpComments",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_comment_moderate"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "comment_id": {
            "type": "integer",
            "description": "Comment id."
          },
          "action": {
            "type": "string",
            "description": "approve|hold|spam|trash|reply."
          },
          "content": {
            "type": "string",
            "description": "Reply body (required when action=reply)."
          }
        },
        "required": [
          "comment_id",
          "action"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_option_get",
      "description": "Read a basic site setting (only an allow-list of general options; suite/plugin options are not exposed). Read-only.",
      "capabilityKeys": [
        "wpcore.wp_option_get"
      ],
      "permission": "wpSettingsRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_option_get"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "Option key (e.g. blogname, blogdescription, timezone_string, date_format, posts_per_page)."
          }
        },
        "required": [
          "key"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_option_set",
      "description": "Change a basic site setting (only an allow-list of general options; suite/plugin options cannot be touched). Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_option_set"
      ],
      "permission": "wpSettingsWrite",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_option_set"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "Option key from the writable allow-list."
          },
          "value": {
            "type": [
              "string",
              "integer",
              "number",
              "boolean"
            ],
            "description": "New value."
          }
        },
        "required": [
          "key",
          "value"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_site_info",
      "description": "Site overview: WordPress version, active theme, language, content counts, active plugins. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_site_info"
      ],
      "permission": "wpSite",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_site_info"
      },
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_menu_list",
      "description": "List navigation menus and their items. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_menu_list"
      ],
      "permission": "wpRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_menu_list"
      },
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_read",
      "description": "Read WooCommerce data: list recent orders or products, or one by id. Read-only. Only when WooCommerce is active.",
      "capabilityKeys": [
        "thirdparty.wc_read"
      ],
      "permission": "wcRead",
      "sideEffect": false,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_read"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "description": "'orders' (default) or 'products'."
          },
          "id": {
            "type": "integer",
            "description": "A single order or product id."
          },
          "search": {
            "type": "string",
            "description": "Product search text."
          },
          "per_page": {
            "type": "integer",
            "description": "Items per page (1-50, default 10)."
          },
          "page": {
            "type": "integer",
            "description": "Page number."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_order_note",
      "description": "Add a note to a WooCommerce order (private, or customer-visible). Side-effect. Only when WooCommerce is active.",
      "capabilityKeys": [
        "thirdparty.wc_order_note"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_order_note"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {
            "type": "integer",
            "description": "Order id."
          },
          "note": {
            "type": "string",
            "description": "Note text."
          },
          "customer_note": {
            "type": "boolean",
            "description": "If true, the note is visible to the customer (default false)."
          }
        },
        "required": [
          "order_id",
          "note"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "seo_get",
      "description": "Read the SEO title, meta description and focus keyword of a post/page. Works with Yoast, Rank Math or SEOPress (auto-detected). Read-only.",
      "capabilityKeys": [
        "thirdparty.seo_get"
      ],
      "permission": "seoRead",
      "sideEffect": false,
      "requiresPlugin": "seo",
      "phpService": {
        "filter": "pfa_seo_agent_api",
        "method": "seo_get"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "post_id": {
            "type": "integer",
            "description": "Post/page id."
          },
          "id": {
            "type": "integer",
            "description": "Alias of post_id."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "seo_set",
      "description": "Optimize a post/page's SEO: set the SEO title, meta description and/or focus keyword. Works with Yoast, Rank Math or SEOPress. Side-effect.",
      "capabilityKeys": [
        "thirdparty.seo_set"
      ],
      "permission": "seoWrite",
      "sideEffect": true,
      "requiresPlugin": "seo",
      "phpService": {
        "filter": "pfa_seo_agent_api",
        "method": "seo_set"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "post_id": {
            "type": "integer",
            "description": "Post/page id."
          },
          "title": {
            "type": "string",
            "description": "New SEO title."
          },
          "description": {
            "type": "string",
            "description": "New meta description."
          },
          "focus_keyword": {
            "type": "string",
            "description": "Focus keyword."
          }
        },
        "required": [
          "post_id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "forms_list",
      "description": "List forms across the installed form plugins (Contact Form 7, Fluent Forms, Gravity Forms, WPForms), noting which support stored entries. Read-only.",
      "capabilityKeys": [
        "thirdparty.forms_list"
      ],
      "permission": "formsRead",
      "sideEffect": false,
      "requiresPlugin": "forms",
      "phpService": {
        "filter": "pfa_forms_agent_api",
        "method": "forms_list"
      },
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "forms_entries",
      "description": "List a form's stored entries (Fluent Forms / Gravity Forms; Contact Form 7 stores none). Read-only.",
      "capabilityKeys": [
        "thirdparty.forms_entries"
      ],
      "permission": "formsRead",
      "sideEffect": false,
      "requiresPlugin": "forms",
      "phpService": {
        "filter": "pfa_forms_agent_api",
        "method": "forms_entries"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "plugin": {
            "type": "string",
            "description": "Optional plugin key: fluentforms | gravityforms."
          },
          "form_id": {
            "type": "integer",
            "description": "Form id."
          },
          "per_page": {
            "type": "integer",
            "description": "Entries per page (1-50, default 20)."
          }
        },
        "required": [
          "form_id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_post_meta_set",
      "description": "Set a PUBLIC custom field (post meta) on a post. Protected keys (starting with _) are refused. Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_post_meta_set"
      ],
      "permission": "wpWriteContent",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_post_meta_set"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Post id."
          },
          "slug": {
            "type": "string",
            "description": "Post slug (needs post_type)."
          },
          "post_type": {
            "type": "string",
            "description": "Post type when resolving by slug."
          },
          "key": {
            "type": "string",
            "description": "Public meta key (no leading underscore)."
          },
          "value": {
            "type": [
              "string",
              "integer",
              "number",
              "boolean"
            ],
            "description": "Value."
          }
        },
        "required": [
          "key",
          "value"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_media_sideload",
      "description": "Download a remote http(s) image/file URL into the media library. SSRF-guarded (rejects private/loopback hosts). Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_media_sideload"
      ],
      "permission": "wpMedia",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_media_sideload"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "Public http(s) URL to import."
          },
          "title": {
            "type": "string",
            "description": "Optional attachment title."
          }
        },
        "required": [
          "url"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_menu_manage",
      "description": "Create a navigation menu (action=create_menu) or append an item to one (action=add_item, with a post_id or a url). Side-effect.",
      "capabilityKeys": [
        "wpcore.wp_menu_manage"
      ],
      "permission": "wpTheme",
      "sideEffect": true,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_menu_manage"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "create_menu | add_item."
          },
          "name": {
            "type": "string",
            "description": "Menu name (create_menu)."
          },
          "menu_id": {
            "type": "integer",
            "description": "Target menu id (add_item)."
          },
          "title": {
            "type": "string",
            "description": "Item title (add_item)."
          },
          "post_id": {
            "type": "integer",
            "description": "Link the item to this post/page (add_item)."
          },
          "url": {
            "type": "string",
            "description": "Custom link URL (add_item)."
          }
        },
        "required": [
          "action"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_widget_list",
      "description": "List widgets grouped by sidebar. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_widget_list"
      ],
      "permission": "wpTheme",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_widget_list"
      },
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_plugins_list",
      "description": "List installed plugins and their active state. Read-only.",
      "capabilityKeys": [
        "wpcore.wp_plugins_list"
      ],
      "permission": "wpSite",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_plugins_list"
      },
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wp_posttypes_list",
      "description": "List registered post types and their taxonomies (discover any plugin's content; then use the wp_post_* tools with the post_type). Read-only.",
      "capabilityKeys": [
        "wpcore.wp_posttypes_list"
      ],
      "permission": "wpRead",
      "sideEffect": false,
      "phpService": {
        "filter": "pfa_wpcore_agent_api",
        "method": "wp_posttypes_list"
      },
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_order_update",
      "description": "Change a WooCommerce order's status (processing/completed/on-hold/…). Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_order_update"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_order_update"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {
            "type": "integer",
            "description": "Order id."
          },
          "status": {
            "type": "string",
            "description": "Target status slug (e.g. processing, completed, on-hold)."
          },
          "note": {
            "type": "string",
            "description": "Optional status-change note."
          }
        },
        "required": [
          "order_id",
          "status"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_order_cancel",
      "description": "Cancel a WooCommerce order. Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_order_cancel"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_order_cancel"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {
            "type": "integer",
            "description": "Order id."
          },
          "reason": {
            "type": "string",
            "description": "Optional reason."
          }
        },
        "required": [
          "order_id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_order_create",
      "description": "Create a pending WooCommerce order, optionally with a customer and line items. Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_order_create"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_order_create"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "customer_id": {
            "type": "integer",
            "description": "Optional customer user id."
          },
          "items": {
            "type": "array",
            "description": "Line items.",
            "items": {
              "type": "object",
              "properties": {
                "product_id": {
                  "type": "integer",
                  "description": "Product id."
                },
                "quantity": {
                  "type": "integer",
                  "description": "Quantity."
                }
              },
              "required": [
                "product_id"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_order_line",
      "description": "Add or remove a product line on a WooCommerce order. Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_order_line"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_order_line"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {
            "type": "integer",
            "description": "Order id."
          },
          "op": {
            "type": "string",
            "description": "add (default) | remove."
          },
          "product_id": {
            "type": "integer",
            "description": "Product id (add)."
          },
          "quantity": {
            "type": "integer",
            "description": "Quantity (add)."
          },
          "item_id": {
            "type": "integer",
            "description": "Line item id (remove)."
          }
        },
        "required": [
          "order_id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_apply_coupon",
      "description": "Apply a coupon code to a WooCommerce order. Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_apply_coupon"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_apply_coupon"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {
            "type": "integer",
            "description": "Order id."
          },
          "code": {
            "type": "string",
            "description": "Coupon code."
          }
        },
        "required": [
          "order_id",
          "code"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_stock_set",
      "description": "Set a product's stock quantity (and derived stock status). Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_stock_set"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_stock_set"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "product_id": {
            "type": "integer",
            "description": "Product id."
          },
          "quantity": {
            "type": "integer",
            "description": "New stock quantity."
          }
        },
        "required": [
          "product_id",
          "quantity"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_product_upsert",
      "description": "Create or edit a simple WooCommerce product (name, price, sku, description, status). Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_product_upsert"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_product_upsert"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Product id to edit (omit to create)."
          },
          "name": {
            "type": "string",
            "description": "Product name."
          },
          "regular_price": {
            "type": "string",
            "description": "Regular price."
          },
          "sku": {
            "type": "string",
            "description": "SKU."
          },
          "description": {
            "type": "string",
            "description": "Description."
          },
          "status": {
            "type": "string",
            "description": "publish|draft|pending."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "wc_refund_request",
      "description": "Record a refund REQUEST on an order for a human to process. NEVER issues the refund automatically (our policy). Side-effect.",
      "capabilityKeys": [
        "thirdparty.wc_refund_request"
      ],
      "permission": "wcWrite",
      "sideEffect": true,
      "requiresPlugin": "woocommerce",
      "phpService": {
        "filter": "pfa_woocommerce_agent_api",
        "method": "wc_refund_request"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {
            "type": "integer",
            "description": "Order id."
          },
          "amount": {
            "type": "number",
            "description": "Requested refund amount."
          },
          "reason": {
            "type": "string",
            "description": "Reason."
          }
        },
        "required": [
          "order_id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "forms_entry_manage",
      "description": "Manage a stored form entry: read/unread, spam/unspam, trash, star/unstar, or add a note (Gravity Forms, Fluent Forms). Side-effect.",
      "capabilityKeys": [
        "thirdparty.forms_entry_manage"
      ],
      "permission": "formsRead",
      "sideEffect": true,
      "requiresPlugin": "forms",
      "phpService": {
        "filter": "pfa_forms_agent_api",
        "method": "forms_entry_manage"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "plugin": {
            "type": "string",
            "description": "gravityforms | fluentforms."
          },
          "entry_id": {
            "type": "integer",
            "description": "Entry id."
          },
          "action": {
            "type": "string",
            "description": "read|unread|spam|unspam|trash|star|unstar|note."
          },
          "note": {
            "type": "string",
            "description": "Note text (action=note)."
          }
        },
        "required": [
          "entry_id",
          "action"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "ld_read",
      "description": "Read LearnDash courses, lessons, or a user's enrollments. Read-only.",
      "capabilityKeys": [
        "thirdparty.ld_read"
      ],
      "permission": "lmsRead",
      "sideEffect": false,
      "requiresPlugin": "lms",
      "phpService": {
        "filter": "pfa_learndash_agent_api",
        "method": "ld_read"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "description": "courses (default) | lessons | enrollments."
          },
          "user_id": {
            "type": "integer",
            "description": "User id (enrollments)."
          },
          "per_page": {
            "type": "integer",
            "description": "Items per page."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "ld_enroll",
      "description": "Enroll or unenroll a user in a LearnDash course. Side-effect.",
      "capabilityKeys": [
        "thirdparty.ld_enroll"
      ],
      "permission": "lmsWrite",
      "sideEffect": true,
      "requiresPlugin": "lms",
      "phpService": {
        "filter": "pfa_learndash_agent_api",
        "method": "ld_enroll"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "user_id": {
            "type": "integer",
            "description": "User id."
          },
          "course_id": {
            "type": "integer",
            "description": "Course id."
          },
          "op": {
            "type": "string",
            "description": "enroll (default) | unenroll."
          }
        },
        "required": [
          "user_id",
          "course_id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "mp_read",
      "description": "Read MemberPress membership products, or a member's active memberships. Read-only.",
      "capabilityKeys": [
        "thirdparty.mp_read"
      ],
      "permission": "membershipRead",
      "sideEffect": false,
      "requiresPlugin": "membership",
      "phpService": {
        "filter": "pfa_memberpress_agent_api",
        "method": "mp_read"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "description": "memberships (default) | member."
          },
          "user_id": {
            "type": "integer",
            "description": "User id (kind=member)."
          }
        },
        "required": [],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    },
    {
      "name": "mp_access",
      "description": "Grant or revoke a member's access to a membership (creates/expires a manual transaction). Sensitive; side-effect.",
      "capabilityKeys": [
        "thirdparty.mp_access"
      ],
      "permission": "membershipWrite",
      "sideEffect": true,
      "requiresPlugin": "membership",
      "phpService": {
        "filter": "pfa_memberpress_agent_api",
        "method": "mp_access"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "user_id": {
            "type": "integer",
            "description": "User id."
          },
          "membership_id": {
            "type": "integer",
            "description": "Membership product id."
          },
          "op": {
            "type": "string",
            "description": "grant (default) | revoke."
          }
        },
        "required": [
          "user_id",
          "membership_id"
        ],
        "additionalProperties": false
      },
      "tests": ["tests-suite/tool-invocation.spec.cjs"]
    }
  ]
}
